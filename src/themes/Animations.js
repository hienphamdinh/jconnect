import launchAnimation from 'assets/animation/launch_animation.json';
import circleAnimation from 'assets/animation/circle-animation.json';

const animations = {
  launchAnimation,
  circleAnimation,
};

export default animations;
